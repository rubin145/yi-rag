.*
*.env
# *.yaml  # Allow config files to be committed
*.parquet
data/raw/*.txt
*.csv
*.lock
*.ipynb

NOTES.md

# Binary model files (downloaded at runtime)
data/sparse_model/tfidf_vectorizer.joblib

# Poetry files (use pip for HF Spaces)
poetry.lock